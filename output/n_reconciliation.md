# Stage N — Reporting-number reconciliation

Generated by `R/stage_dn.R` (seed 0). Tables: `n_reconciliation.csv`,
`n_label_audit.csv`.

## Confirmed counts (all match `assumptions.md` §14 exactly)

| quantity | value | kind | estimation N? |
|---|---:|---|:--:|
| file rows (`stacked_fatal.csv`) | 278,324 | file_rows | no |
| outcome-observed rows | 171,906 | file_rows (outcome-observed) | no |
| — control rows (741×21×11) | 171,171 | file_rows (control only) | no |
| — treated outcome rows | 735 | file_rows (treated) | no |
| **felm N, plain spec** | **171,906** | **estimation_N** | **yes** |
| **felm N, covariate spec (m3/m4)** | **171,444** | **estimation_N** | **yes** |
| rows dropped (missing `pct.white.officers.imputed`) | 462 | dropped_rows | no |

171,906 = 171,171 control + 735 treated. The covariate spec drops 462 of those
for a missing officer-race covariate → **171,444** is the estimation N of Table 3's
`m3`/`m4`, **not** a row count. `n_reconciliation.csv` also carries the Cengiz
windowed **row counts** (33k–48k; 14k at ±9) with `kind = row_count_windowed`, so
the one table holds every number the slides place side by side, each typed.

## Labeling audit of `output/` (`n_label_audit.csv`)

**Collisions found: 0.** No file in `output/` labels a windowed row count and an
estimation N with the same label.

| file | "Rows"-type label | carries estimation N | windowed row count | collision |
|---|:--:|:--:|:--:|:--:|
| stacked_dimensionality.{csv,tex} | yes | no | yes (33k–48k) | no |
| ladder.{csv,md} | n_obs only | yes | no | no |
| control_pool_verification.{csv,md} | estimation_rows | yes | no | no |
| n_reconciliation.csv | via `kind` col | yes | yes | no |

**Finding:** the mislabeling the prompt warns about — 171,444 (an estimation N)
presented under a "Rows" heading next to the 40–48k windowed row counts — does
**not** occur anywhere in `output/`. `stacked_dimensionality` correctly labels its
33k–48k values as "Rows" (they are file row counts) and contains no estimation N;
the estimation Ns (171,444 / 171,906) appear only under estimation-N labels
(`n_obs`, `felm_N_*`, `estimation_rows`). The confusion originates in the paper's
**slides** (outside this repo's `output/`); this reconciliation table is the
single typed source of truth to replace that labeling.
